services:
  node:
    environment:
      - CI
      - DB_NAME=realworld_test
      - NODE_ENV=test
      - PINO_LEVEL=silent
  postgres:
    volumes:
      - test_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=realworld_test

volumes:
  test_data:
